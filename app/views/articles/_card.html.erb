<div class="card py-2 px-2 mb-2 block_click" data-href="{{ article.path }}">
	<div class="align-items-start d-flex">

		<a href="{{ article.path }}">
			<img ng-src="{{article.image}}" class="rounded feed_image" alt="{{article.title}}" />
		</a>

		<div class="w-100">
			<div class="d-flex align-items-start justify-content-between">
				<div>
					<h5 class="card-title d-flex justify-content-between mb-1">
						<a href="{{ article.path }}">
							<span>{{ article.title }}</span>
						</a>
					</h5>
					<h6 class="mb-1">
						<a href="{{ article.path }}">
							<span>{{ article.author }}</span>
						</a>
					</h6>
				</div>
			</div>

			<div class="pb-0 pt-1">
				<p class="card-text">
					<a href="{{ article.path }}">
						<small>{{ article.snippet }}</small></p>
					</a>
			</div>
		</div>

	</div>
</div>