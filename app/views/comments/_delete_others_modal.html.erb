<div class="modal fade no_connection question_modal" id="deleteOthersCommentModal" tabindex="-1" role="dialog">
  <div class="modal-dialog medium" role="document">
    <div class="modal-content">

    	<div class="modal-header">
    		<h5 class="pt-2">
          Delete comment
        </h5>

        <button data-dismiss="modal" class="btn btn-link close_modal">
          <i class="fas fa-times"></i>
        </button>
    	</div>

      <div class="modal-body pb-3" id="delete_others_comment_modal">
      	<section class="container my-3 px-4">
      		<p>You are about to delete a comment made by <b>{{dataForCommentDeletion.comment.data.username}}</b> and any replies made to that comment. Please tell us why:</p>

					<div class="options">
						<div class="form-group">
	            <label class="option">
	              <input type="radio" name="delete_comment_reason" ng-model="dataForCommentDeletion.comment.data.deleteReason" value="offensive" />
	              <span class="checkmark"></span>
	              <span class="text">It’s offensive</span>
	            </label>
	          </div>

	          <div class="form-group">
	            <label class="option">
	              <input type="radio" name="delete_comment_reason" ng-model="dataForCommentDeletion.comment.data.deleteReason" value="dont_agree" />
	              <span class="checkmark"></span>
	              <span class="text">I do not agree with the comment</span>
	            </label>
	          </div>

	          <div class="form-group">
	            <label class="option">
	              <input type="radio" name="delete_comment_reason" ng-model="dataForCommentDeletion.comment.data.deleteReason" value="other" />
	              <span class="checkmark"></span>
	              <span class="text">Other</span>
	            </label>
	          </div>

	          <div class="form-group mt-4" ng-show="dataForCommentDeletion.comment.data.deleteReason == 'offensive'">
	            <label class="">
	              <input type="checkbox" name="also_block" ng-model="dataForCommentDeletion.comment.data.deleteAlsoBlock" value="1" />
	              <small class="pl-2">I would also like to block <b>{{dataForCommentDeletion.comment.data.username}}</b></small>
	            </label>

	            <label class="">
	              <input type="checkbox" name="also_report" ng-model="dataForCommentDeletion.comment.data.deleteAlsoReport" value="1" />
	              <small class="pl-2">I would also like to send a concern report regarding <b>{{dataForCommentDeletion.comment.data.username}}'s comment</b></small>
	            </label>

	          </div>
	        </div>

      		<div class="buttons">
      		  <button class="btn btn-block btn-success mt-4"
      		  				ng-click="deleteCommentFromModal($event, dataForCommentDeletion.item, dataForCommentDeletion.comment, dataForCommentDeletion.parent)"
      		  				ng-disabled="!dataForCommentDeletion.comment.data.deleteReason"
      		          ng-class="{'disabled': !dataForCommentDeletion.comment.data.deleteReason}">Submit</button>
      		</div>

      	</section>
      </div>
    </div>
  </div>
</div>
