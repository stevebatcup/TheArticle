<div class="card py-2 px-2 mb-2 block_click" data-href="{{ item.path }}">
	<div class="align-items-start d-flex">

		<a href="{{ item.path }}">
			<img ng-src="{{item.image}}" class="feed_image" alt="{{item.name}}" />
		</a>

		<div class="w-100">
			<div class="d-flex align-items-start justify-content-between">
				<div>
					<h4 class="card-title d-flex justify-content-between mb-1 pt-2">
						<a href="{{ item.path }}" class="pt-1">
							<span>{{ item.name }}</span>
						</a>
					</h4>
					<h6 class="text-muted mb-1">
						<a href="{{ item.path }}">
							<span>{{ item.articleCount }}</span>
						</a>
					</h6>
				</div>
			</div>
		</div>

	</div>
</div>