<!-- Teads -->
<script type="text/javascript" class="teads" src="//a.teads.tv/page/116407/tag" async="true"></script>
<script type="text/javascript">
  var articleDataForAds = [];
  var passbackHtml;
  <% if is_article_page? && @article.present? %>
    articleDataForAds.push(<%= @article.id %>);
    articleDataForAds.push("<%= yield_page_title(default_page_title) %>");
    passbackHtml = "<script type='text\/javascript'> var googletag = googletag || {}; googletag.cmd = googletag.cmd || []; window.googletag = googletag; googletag.cmd.push(function() { googletag.defineSlot('/<%= ad_publisher_id %>/thearticle_article/unruly', [2, 2], 'div-gpt-ad-<%= ad_page_id %>-4').addService(googletag.pubads()); googletag.pubads().setTargeting('article_id', articleDataForAds[0]); googletag.pubads().setTargeting('member', 'true'); googletag.pubads().setTargeting('content', articleDataForAds[1]); googletag.pubads().enableSingleRequest(); googletag.pubads().collapseEmptyDivs(); googletag.enableServices();});<\/script>";
  <% end %>
  var d = window.document;
  try {
    d = window.top.document;
  } catch (e) {}
  var adFrame = d.createElement("iframe");
  adFrame.width = 2;
  adFrame.height = 2;
  adFrame.border = 0;
  adFrame.style.border = "0";
  adFrame.frameBorder = 0;
  adFrame.scrolling = "no";
  adFrame.style = "display: block; margin: auto;";
  var passbackSlot = d.querySelectorAll("#content_bar #content_box > p")[1];
  passbackSlot.appendChild(adFrame);
  adFrame.contentWindow.document.open("text/html");
  adFrame.contentWindow.document.write(passbackHtml);
  adFrame.contentWindow.document.close();
</script>