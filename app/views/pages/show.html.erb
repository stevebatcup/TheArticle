<% page_title @page.title.html_safe %>
<% meta_tag :description, @page.meta_description %>

<div id="content-page" ng-controller="ContentPageController">
	<article>
		<header class="entry-header">
			<h1 class="">
				<div class="container">
					<%= @page.title.html_safe %>
				</div>
			</h1>
		</header>

		<div class="entry-content container">
			<%= @page.content.html_safe %>
		</div>
	</article>
</div>